This is a quiz app 
Foloow the steps to run this:
Open cmd (Win + R)
Type cmd
Type thix
      npx create-react-app quiz-app
      cd quiz-app
      cd src
      notepad App.js 
      cd ..
      npm start
This will open (http://localhost:3000/)
